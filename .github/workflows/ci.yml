name: CI

on:
  push:
    branches: ["main"]
  pull_request:

jobs:
  python:
    strategy:
      fail-fast: false
      matrix:
        os: [windows-latest]
        python-version: ["3.12", "3.14"]

    runs-on: ${{ matrix.os }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install -e ".[dev]"

      - name: Lint
        run: python -m ruff check .

      - name: Compile apps
        run: python -m py_compile apps\desktop\app.py apps\streamlit\app.py localdoc_markdown\core.py

      - name: Check dependencies
        run: python -m pip check

      - name: Test
        run: python -m pytest
